import React from 'react';
import Table from '../Table/OrdersTable';

const Orders = () => {
  return (
    <div>
      <Table/>
    </div>
  );
};
{/* <UilSignOutAlt className="icon" onClick={() => dispatch(logout())} /> */}

export default Orders;
